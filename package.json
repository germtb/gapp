{
  "private": true,
  "workspaces": [
    "client",
    "react",
    "codegen"
  ],
  "scripts": {
    "build": "npm run build --workspace=client && npm run build --workspace=react",
    "typecheck": "npm run typecheck --workspace=client && npm run typecheck --workspace=react"
  }
}
